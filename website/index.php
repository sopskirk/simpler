<?php
/**
 * 
 * In this file you can put references 
 * to all of your page files.
 * 
 * @package Simpler
 * @subpackage Website
 * 
 */


    $file = using('file');
    $cookie = using('cookie');
    $rand = using('randval');

    //$cookie->create('cookie_name', 'cookie_value', '10 days')->save();

    /* File class methods
    $file->put();
    $file->use();
    $file->prepend();
    $file->append();
    $file->access();
    $file->move();
    $file->copy();
    $file->clean();
    $file->rename();
    $file->newLine();
    $file->content();
    $file->length();
    $file->size();
    $file->lastModified();
    $file->mode();
    $file->path();
    $file->find();
    $file->findFirst();
    $file->findLast();
    $file->findAll();
    $file->trash();
    $file->isExists();
    $file->isEmpty();

    // Cookie class methods
    $cookie->create();
    $cookie->use();
    $cookie->get();
    $cookie->update();
    $cookie->has();
    $cookie->trash();
    $cookie->clearStorage();
    $cookie->preview();
    $cookie->create()->save();

    // Instances class report and import
    import();
    report();

    // Randval class methods
    $rand->generate();
    $rand->bytes();
    $rand->unique();*/
   
    



